import React from "react";
import LoginPage from "../components/auth/loginPage";

export default {
  title: "Learnbird/Components",
  component: LoginPage,
};

const LoginPageComponent = (args) => <LoginPage />;

export const LoginPageSample = LoginPageComponent;
